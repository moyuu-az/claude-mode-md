# Security Rules

- .env / credentials は絶対にコミットしない
- 全ユーザー入力を信頼境界でスキーマバリデーションする（ライブラリは言語別 rules に従う）
- SQL インジェクション, XSS, CSRF を常に意識
- 環境変数で機密情報を管理（ハードコード禁止）
- API エンドポイントにはレート制限を設定
- エラーメッセージに内部情報を漏らさない（production では汎用メッセージ）
